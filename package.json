{
  "name": "semantic-release-dependabot",
  "version": "0.0.1",
  "description": "A simple Semantic Release plugin for updating private dependencies in Dependabot.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@jtferns",
  "license": "MIT",
  "dependencies": {
    "@semantic-release/error": "^2.2.0",
    "node-fetch": "^2.6.0"
  }
}
